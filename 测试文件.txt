nginx:服务端的负载均衡器：用户连接的服务器设备都保存在服务器本地，nginx是专门的一台服务器，保存IP:端口
niginx统一负责管理调度，有1000个请求也需要一个一个的进行分配
Ribbon是客户端的负载均衡器，有几个消费者就有几个Ribbon，且数据信息保存在消费者本地。
当用户每次发起一个请求的时候，用户就已经知道要访问哪一台服务器：不是集中式的。每一个用户都有各自的负载均衡机制。
相对于Nginx而言，Riboon的效率更高。
假如Nginx宕机，全部用户都受影响。如果某一个消费节点宕机，不会影响其它消费者，每一个消费者都有各自的负载均衡。